<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <%= link_to 'Blog', root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="nav-item active"><%= link_to t('navigation.links.posts'), posts_path, class: "nav-link" %></li>
        <li class="nav-item active"><%= link_to t('navigation.links.favorite_posts'), favorite_posts_path, class: "nav-link" %></li>
        <li class="nav-item active"><%= link_to t('navigation.links.discarded_posts'), discard_posts_path, class: "nav-link" %></li>
        <li class="nav-item active"><%= link_to t('navigation.links.create_post'), new_post_path, class: "nav-link" %></li>
      </ul>
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li><%= link_to t('navigation.links.logout'), destroy_user_session_path, :method => :delete %></li>
        <% else %>
          <li><%= link_to t('navigation.links.login'), new_user_session_path %></li>
          <li><%= link_to t('navigation.links.register'), new_user_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>